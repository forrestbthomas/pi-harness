.pi/sessions/
eval/.venv/
eval/__pycache__/
eval/tests/__pycache__/
eval/.pytest_cache/
eval/outputs/
# Benchmark run reports (JSON)
eval/benchmark-results/
# Nightly live eval results (pytest-json-report + score_run.py summaries)
eval/live-results/
.env
*.pyc
.DS_Store

# Root-level test caches (eval/ variants covered below)
.pytest_cache/

# Generated `pi-run project-understand` output (regenerable)
docs/understand/
# Git worktrees for parallel agent work
.worktrees/

# subagent-driven-development progress ledger
.superpowers/

# Go build output
bin/

# pi-subagents runtime artifacts (may contain sensitive session transcripts)
.pi-subagents/

# Spend ledger (local cost tracking; per-run spend totals)
.pi/cost-ledger.jsonl
.pi/cost-ledger-*.archive.jsonl
.pi/cost-ledger.reset

# DeepEval cache
eval/.deepeval/
.deepeval/

# Local multi-axis review sweep artifacts (gpt-5.6-terra)
.review-sweep/
