# Python dependencies for the DeepEval evaluation suite.
# This suite intentionally uses the DeepEval + pytest stack: deepeval, pytest,
# pytest-timeout, pypdf, and python-dotenv. Ranges use ~= (compatible release)
# bounds for reproducibility. Add a new dependency only by deliberate decision:
# update this policy comment and the README dependency-policy line together.
# pytest-json-report~=1.5 is retained for shape-compatibility with the scorer
# fixtures; the nightly report is written by eval/conftest.py's own
# pytest_runtest_logreport hook via PI_EVAL_REPORT (the plugin silently fails
# to write under pytest 9.1.1, per conftest.py).
# Run `pi-run setup` to install into eval/.venv.
deepeval~=4.1
pytest~=9.1
python-dotenv~=1.2
pypdf~=6.15
pytest-timeout~=2.4
pytest-json-report~=1.5
