<!-- Managed by lore (https://github.com/BYK/loreai) — manual edits are imported on next session. -->

## Long-term Knowledge

### Decision

<!-- lore:019fc3e0-124e-70e5-ab8b-3def5cfd93a2 -->
* **Decision to make pi-run the single harness entry point**: The Pi harness runtime is a Go CLI (, module ) — the single source of truth for provider routing (OpenAI default; OpenRouter and DeepSeek direct via /), Bitwarden key resolution (env → , never printed), Pi launching (nvm-aware), eval, setup, doctor. Replaced all shell functions (/ in /) and Makefile targets; no cross-provider auto-fallback (explicit routing only).  and chatbot projects moved to standalone repos (~/Projects).
