The root cause is in eval/tests/test_live_metrics.py: _DATASET holds only the judge-graded subset (5 rows) while the sample_cases fixture holds the full dataset (54 rows), and both are indexed by the same idx — so each judge case is graded against a different case's input and expected_output. The version differences (deepeval 4.1.8 vs 4.1.7, pi, Node) were red herrings, not the cause.
