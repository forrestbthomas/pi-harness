# Python dependencies for the DeepEval evaluation suite.
# This suite intentionally uses the DeepEval + pytest stack: deepeval, pytest,
# pytest-timeout, pypdf, and python-dotenv. Ranges use ~= (compatible release)
# bounds for reproducibility. Add a new dependency only by deliberate decision:
# update this policy comment and the README dependency-policy line together.
# pytest-json-report~=1.5 is a deliberate addition for the nightly live-eval
# gate: score_run.py consumes its JSON report to rebuild per-case aggregates
# and baseline comparisons (live-agent-eval-v2 spec §5.4). It is small, widely
# used, and its tests are hermetic (fixture JSON only, no keys, no network).
# The README dependency-policy line for pytest-json-report is updated by the
# docs pass (parent-owned).
# Run `pi-run setup` to install into eval/.venv.
deepeval~=4.1
pytest~=9.1
python-dotenv~=1.2
pypdf~=6.15
pytest-timeout~=2.4
pytest-json-report~=1.5
